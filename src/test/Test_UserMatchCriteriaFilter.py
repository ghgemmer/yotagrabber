import pandas
import sys

def filterDataFrame(df):
    dfFiltered = df
    return dfFiltered
    
def criteriaPrintableString():
    # returns a string of the printed criteria
    criteriaStr = ""
    criteriaStr += 'Match criteria is No filterning'
    return criteriaStr
